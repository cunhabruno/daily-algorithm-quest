from setuptools import setup, find_packages

setup(
    name="daily_algos_quest",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
    test_suite='inittest'
)